# NOTE Project


## First start

go to folder root
docker compose up --build -d

## Pure all services includes Database and start 
go to folder root
docker compose down --volumes --remove-orphans
docker compose up --build -d
docker compose up -d